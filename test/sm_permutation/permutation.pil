constant %N = 2**10;

namespace Global(%N);
    pol constant L1;

namespace Permutation(%N);

    pol commit a, b;
    pol commit c, d;
    pol commit selC, selD;

    a is b;
    selC {c} is selD {d};

