
constant %N = 2**10;

namespace Global(%N);
    pol constant L1;

namespace Plookup(%N);


    pol commit sel, a, b;
    
    pol constant SEL, A, B;
    pol commit cc;

    sel {a, b', a*b'} in SEL { A, B, cc};


