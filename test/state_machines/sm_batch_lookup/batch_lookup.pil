namespace BatchLookup(%N);

    pol commit f[10];
    pol commit t[2];

    {f[0], f[1]} in {t[0], t[1]};
    {f[2], f[3]} in {t[0], t[1]};
    {f[4], f[5]} in {t[0], t[1]};
    {f[6], f[7]} in {t[0], t[1]};
    {f[8], f[9]} in {t[0], t[1]};

